export default interface order {
  country: string | void;
  deliveryMethod: string | void;
  email: string | void;
  name: string | void;
  street: string | void;
  zipCode: string | void;
  id: string;
}
